import { Box, styled } from "@mui/material";

export const StyledAuthBox = styled(Box)(() => ({
    width: "100%",
    mx: "auto",
    mt: 10,
    display: "flex",
    flexDirection: "column",
    gap: 2,
}));